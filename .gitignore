# Secrets
.env
.env.local
.env*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
# Anchored: an unanchored `build/` also matches web/app/build/, which silently drops the
# prospect builder route from git AND from Tailwind's source scan (it honours .gitignore).
/build/
dist/
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
